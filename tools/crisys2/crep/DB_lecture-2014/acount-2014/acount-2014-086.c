#include <stdio.h>

int main()
{
  char str[100], count[];
  int i, a=0, b =0, c=0, d=0, e=0, f =0, g=0, h=0, ii=0, j=0, k=0, l=0; 
  int m=0, n=0, o=0, p=0, q=0, r=0, s=0, t=0, u=0, v=0, w=0, x=0, y=0;
  int g=0, kk, max;
  printf("文字列を入力してください（長さ１００以下)");
  gets(str);
  for(i = 0; str[i] != '\0'; i++){
    if(str[i] == 'A' || 'a')
      a++;

    else if(str[i] == 'B' || 'b')
      b++;

    else if(str[i] == 'C' || 'c')
      c++;


    else if(str[i] == 'D' || 'd')
      d++;

    else if(str[i] == 'E' || 'e')
      e++;

    else if(str[i] == 'F' || 'f')
      f++;

    else if(str[i] == 'G' || 'g')
      g++;

    else if(str[i] == 'H' || 'h')
      h++;

    else if(str[i] == 'I' || 'i')
      ii++;

    else if(str[i] == 'J' || 'j')
      j++;

    else if(str[i] == 'K' || 'k')
      k++;

    else if(str[i] == 'L' || 'l')
      l++;

    else if(str[i] == 'N' || 'n')
      n++;

    else if(str[i] == 'O' || 'o')
      o++;

    else if(str[i] == 'M' || 'm')
      m++;

    else if(str[i] == 'P' || 'p')
      p++;

    else if(str[i] == 'Q' || 'q')
      q++;

    else if(str[i] == 'R' || 'r')
      r++;

    else if(str[i] == 'S' || 's')
      s++;

    else if(str[i] == 'T' || 't')
      t++;

    else if(str[i] == 'U' || 'u')
      u++;

    else if(str[i] == 'V' || 'v')
      v++;

    else if(str[i] == 'w' || 'W')
      w++;

    else if(str[i] == 'X' || 'x')
      x++;

    else if(str[i] == 'Y' || 'y')
      y++;

    else if(str[i] == 'G' || 'g')
      g++;}
  
  kk = 0;
  count[] = a,b,c,d,e,f,g,h,ii,j,k,l,n,m,o,p,q,r,s,t,u,v,w,x,y,g;
    for(kk = 0;;kk++){
      kk < kk + count[];
      max = kk;
    }
      printf("頻出文字は %d",max);
      printf("出現回数は%d回", count[]);
      
}
