#include <stdio.h>
#include <string.h>

int main(void)
{
  int i; 
  char c[100];
  printf("文字列を入力してください:");
  sacnf("%s", &c);
}


  int let_cnt(char str[], char c)
  {
if (str[i] >= 'A' && str[i] <= 'Z')      
    num = str[i] - 'A';
else if(str[i] >= 'a' && str[i] <= 'z'){
       num = str[i] - 'a';
       for (t = 0; t < 26; t++){
	 if ( t == num)
	   count[t]++;
       }}
 return 0;
  }
