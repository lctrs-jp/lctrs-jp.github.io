#include <stdio.h>
int main()
{
  char str[40];
  int i,j,a;
  
  printf("文字列を入力してください");
  scanf("&s", &str);
  for(i=0; str[i]!='\0'; i++)
    if ('A'<=str[i] && str[i]<='z')
        
  }
  printf("")
    return 0;
} 
